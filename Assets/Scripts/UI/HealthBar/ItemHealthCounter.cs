using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ItemHealthCounter : MonoBehaviour
{
    public bool firstSphereFilled;
    public bool lowSphereFilled;
    public bool middleSphereFilled1;
    public bool middleSphereFilled2;

    // Start is called before the first frame update
    void Start()
    {
        firstSphereFilled = true;
    }

    // Update is called once per frame
    
}
